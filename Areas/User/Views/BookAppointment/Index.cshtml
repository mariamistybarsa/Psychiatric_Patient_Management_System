
//Index
@model List<Psychiatrist_Management_System.Models.BookingHistoryVM>

<h3 class="mb-4">Booking History</h3>
<form method="get" asp-controller="PatientList" asp-action="Search" class="d-flex mb-3">
    <input type="text" name="UserName" class="form-control me-2" placeholder="Search by ID, Name, or Email" />
    <button type="submit" class="btn btn-primary">Search</button>
</form>
@if (TempData["Message"] != null)
{
    <div class="alert alert-info text-center">
        @TempData["Message"]
    </div>
}

@if (Model != null && Model.Count > 0)
{
    <div class="row">
        @foreach (var item in Model)
        {
            <div class="col-md-6 mb-3">
                <div class="card shadow-sm">
                    <div class="card-header bg-primary text-white">
                        Booking History:@item.AppointmentDay
                    </div>
                    <div class="card-body">
                        <p><strong>Psychiatrist:</strong> @item.PsychiatristName</p>
                        <p><strong>Patient:</strong> @item.PatientName</p>
                        <p><strong>Date:</strong> @item.AppointmentDate.ToString("dd-MM-yyyy")</p>
                        <p><strong>Time:</strong> @item.AppointmentTime</p>
                        <p><strong>Day:</strong> @item.AppointmentDay</p>
                        <p><strong>Notes:</strong> @item.Notes</p>
                        <p>
                            <strong>Approval Status:</strong>
                            <span class="badge @(item.ApprovalStatus == "Pending" ? "bg-warning text-dark" :
                                                                                    item.ApprovalStatus == "Rejected" ? "bg-danger" :
                                                                                    "bg-success")">
                        @item.ApprovalStatus
                    </span>
                </p>
                <p>
                    <strong>Payment Status:</strong>
                    <span class="badge @(item.PaymentStatus == "Paid" ? "bg-success" : "bg-secondary")">
                        @item.PaymentStatus
                    </span>
                </p>

                        @* Buttons based on status *@
                @if (item.ApprovalStatus == "Pending" || item.ApprovalStatus == "Rejected")
                        {
                            <form asp-action="CancelBooking" method="post" onsubmit="return confirm('Are you sure you want to cancel this booking?');">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="bookingId" value="@item.BookingId" />
                                <button type="submit" class="btn btn-danger btn-sm">Cancel Booking</button>
                            </form>
                        }
                        else if (item.ApprovalStatus == "Approved" && item.PaymentStatus == "NotPaid")
                        {
                            @* <form asp-action="PayBooking" method="post">
                                @Html.AntiForgeryToken()
                                <input type="hidden" name="bookingId" value="@item.BookingId" />
                                <button type="submit" class="btn btn-success btn-sm">Pay Now</button>
                            </form> *@
                         


                        <!-- Pay Now button triggers the modal -->
                        <button type="button" class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#paymentModal-@item.BookingId">
                            Pay Now
                        </button>


                        <div class="modal fade" id="paymentModal-@item.BookingId" tabindex="-1" aria-labelledby="paymentModalLabel-@item.BookingId" aria-hidden="true">
                            <div class="modal-dialog modal-dialog-centered">
                                <div class="modal-content">
                                    <div class="modal-header bg-primary text-white">
                                        <h5 class="modal-title" id="paymentModalLabel-@item.BookingId">Payment</h5>
                                        <button type="button" class="btn-close" data-bs-dismiss="modal"></button>
                                    </div>
                                    <div class="modal-body">
                                        <div class="mb-3 border p-2 rounded bg-light">
                                            <h6 class="text-primary">Psychiatrist Information</h6>
                                            <p><strong>Name:</strong> @item.PsychiatristName</p>
                                            <p><strong>Specialization:</strong> @item.Specialization</p>
                                            <p><strong>Experience:</strong> @item.Experience</p>
                                            <p><strong>Emergency Contact:</strong> @item.EmergencyContact</p>
                                        </div>
                                            <!-- Appointment Info -->
                                            <div class="mb-3 border p-2 rounded bg-light">
                                                <h6 class="text-primary">Appointment Information</h6>
                                                <p><strong>Date:</strong> @item.AppointmentDate.ToString("dd-MM-yyyy")</p>
                                                <p><strong>Time:</strong> @item.AppointmentTime</p>
                                                <p><strong>Day:</strong> @item.AppointmentDay</p>
                                                <p><strong>Notes:</strong> @item.Notes</p>
                                            </div>
                                        <!-- Payment Form Here -->
                         

                                        <!-- Payment Options -->
                                        <div class="mb-3">
                                            <label class="form-label">Choose Payment Method:</label>
                                            <div class="d-flex gap-2 flex-wrap">
                                                    <a asp-action="BkashPay" asp-route-bookingId="@item.BookingId" id="bKash_button" class="btn btn-outline-primary">Bkash</a>
                                                <button type="button" class="btn btn-outline-success">Nagad</button>
                                                <button type="button" class="btn btn-outline-info">PayPal</button>
                                                <button type="button" class="btn btn-outline-warning">Rocket</button>
                                            </div>
                                        </div>

                                        <!-- Payment form -->
                                        <form>
                                            <div class="mb-3">
                                                <label for="cardNumber-@item.BookingId" class="form-label">Card Number</label>
                                                <input type="text" class="form-control" id="cardNumber-@item.BookingId" placeholder="1234 5678 9012 3456">
                                            </div>
                                            <div class="mb-3">
                                                <label for="expiry-@item.BookingId" class="form-label">Expiry Date</label>
                                                <input type="month" class="form-control" id="expiry-@item.BookingId" placeholder="MM/YY">
                                            </div>
                                            <div class="mb-3">
                                                <label for="cvv-@item.BookingId" class="form-label">CVV</label>
                                                <input type="text" class="form-control" id="cvv-@item.BookingId" placeholder="123">
                                            </div>
                                        </form>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
                                        <button type="button" class="btn btn-primary">Pay Now</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                            <small class="text-danger">Please make the payment within 24 hours</small>
                        }



                    </div>
                </div>
            </div>
        }
    </div>
}
else
{
    <div class="alert alert-warning text-center">
        No booking history found.
    </div>
}


<!-- Make sure to include Bootstrap CSS & JS -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
