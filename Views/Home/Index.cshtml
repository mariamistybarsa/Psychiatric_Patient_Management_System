<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Psychiatrist Management System</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Inter', sans-serif;
            color: #0f172a;
            min-height: 100vh;
            background: url('https://images.pexels.com/photos/5699456/pexels-photo-5699456.jpeg') center/cover no-repeat;
        }

        /* Soft overlay for readability */
        .overlay {
            background-color: rgba(255, 255, 255, 0.8);
            min-height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
            padding: 2rem;
        }

        h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 1rem;
            color: #1e3a8a;
        }

        p {
            font-size: 1.25rem;
            margin-bottom: 1.5rem;
        }

        .btn-primary {
            background-color: #1e40af;
            border-color: #1e40af;
            font-weight: 500;
        }

            .btn-primary:hover {
                background-color: #1e3a8a;
                border-color: #1e3a8a;
            }

     
    </style>
</head>
<body>
    <div class="overlay">
        <h1>Psychiatrist Management System</h1>
        <p>Connecting you with certified psychiatrists for better mental health care.</p>
        <a href="#contact" class="btn btn-primary btn-lg">Get Started</a>
    </div>

    
</body>
</html>
